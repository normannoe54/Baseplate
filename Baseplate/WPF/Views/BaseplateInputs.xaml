<UserControl x:Class="WPF.Views.BaseplateInputs"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Expander x:Name="Baseplate" Header="Baseplate" Height="312" Margin="15,10,5,0" VerticalAlignment="Top" HorizontalAlignment="Stretch" IsExpanded="True">
            <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="5,0,10,0" >
                <CheckBox x:Name="Enable" Content="Enable"/>
                <StackPanel Orientation="Horizontal">
                    <Label x:Name="ThicknessLabel" Content="Thickness"/>
                    <TextBox x:Name="ThicknessText" TextWrapping="Wrap" Text="{Binding Value, ElementName=ThicknessSlider}"/>
                </StackPanel>
                <Slider x:Name="ThicknessSlider" LargeChange="0.25" Minimum="0.75" Maximum="4"/>
            </StackPanel>
        </Expander>

    </Grid>
</UserControl>
